use Base/Nat/ as N/

// Addition of nats.
// - m: The 1st nat.
// - n: The 2nd nat.
// = The sum of m and n.
Base/Nat/add
: ∀(m: N/Nat)
  ∀(n: N/Nat)
  N/Nat
= λ{
  #zero: λn n
  #succ: λm.pred λn #succ{(Base/Nat/add m.pred n)}
}
