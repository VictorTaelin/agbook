use Base/Char/ as C/
use Base/Bool/ as B/

// Checks if a character is a lowercase letter.
// - c: The character to check.
// = True if the character is lowercase, False otherwise.
C/is-lower
: ∀(c: C/Char)
  B/Bool
= λc (U32/gte c 97) && (U32/lte c 122)